services:
  # =============================================
  # PostgreSQL Master (Primary) - HOM
  # =============================================
  hom-demanda-psql:
    image: postgres:18.4-alpine
    container_name: hom-demanda-psql
    restart: unless-stopped
    env_file:
      - .env.docker
    environment:
      POSTGRES_INITDB_ARGS: "--locale=en_US.utf8 --data-checksums"
    ports:
      - "5438:5432"
    volumes:
      - hom_demanda_psql_data:/var/lib/postgresql
      - ./docker/psql/01-init-master.sh:/docker-entrypoint-initdb.d/00-init-master.sh
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - hom-demanda-net

  # =============================================
  # PostgreSQL Slave (Replica) - HOM
  # =============================================
  hom-demanda-psql-rep:
    image: postgres:18.4-alpine
    container_name: hom-demanda-psql-rep
    restart: unless-stopped
    env_file:
      - .env.docker
    ports:
      - "5439:5432"
    volumes:
      - hom_demanda_psql_rep_data:/var/lib/postgresql
      - ./docker/psql-rep/init-replica.sh:/docker-entrypoint-initdb.d/00-init-replica.sh
    depends_on:
      hom-demanda-psql:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${REPL_USER:-$${POSTGRES_USER}}"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 60s
    networks:
      - hom-demanda-net

  # =============================================
  # Redis - HOM
  # =============================================
  hom-demanda-redis:
    image: redis:8.0-alpine
    container_name: hom-demanda-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports:
      - "6382:6379"
    volumes:
      - hom_demanda_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - hom-demanda-net

volumes:
  hom_demanda_psql_data:
    name: hom_demanda_psql_data
    external: true
  hom_demanda_psql_rep_data:
    name: hom_demanda_psql_rep_data
    external: true
  hom_demanda_redis_data:
    name: hom_demanda_redis_data
    external: true

networks:
  hom-demanda-net:
    name: hom-demanda-net
    driver: bridge
    external: true